{
  "name": "light-orm-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter @sakshamthakur/light-orm build",
    "test": "pnpm --filter @sakshamthakur/light-orm test",
    "typecheck": "pnpm -r typecheck",
    "dev:api": "pnpm --filter todo-api dev",
    "dev:web": "pnpm --filter todo-web dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
